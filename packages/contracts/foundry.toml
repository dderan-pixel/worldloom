[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.20"
optimizer = true
optimizer_runs = 200
via_ir = false

[rpc_endpoints]
base_testnet = "${RPC_URL}"
local = "http://localhost:8545"

[etherscan]
base_testnet = { key = "${ETHERSCAN_API_KEY}" }

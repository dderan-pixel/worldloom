{
  "name": "@predictx/contracts",
  "version": "1.0.0",
  "description": "PredictX Smart Contracts",
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "deploy:local": "forge script script/Deploy.s.sol:DeployScript --rpc-url http://localhost:8545 --broadcast",
    "deploy:testnet": "forge script script/Deploy.s.sol:DeployScript --rpc-url $RPC_URL --broadcast --verify",
    "clean": "forge clean"
  }
}
